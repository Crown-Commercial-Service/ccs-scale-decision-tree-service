# Prevent Lombok looking in higher directories
config.stopBubbling = true

# Instruct lombok to augment constructors with  @ConstructorProperties.
# This facilitates deserialization by Jackson into immutable objects
lombok.anyConstructor.addConstructorProperties = true